import React from "react";

const Home = () => (
  <div>
    <h1>Welcome to test React App with React Router v4</h1>
    <p>Select a page with component you want to visit</p>
  </div>
)

export default Home;
